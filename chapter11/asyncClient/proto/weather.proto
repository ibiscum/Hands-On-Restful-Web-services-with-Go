syntax = "proto3";
option go_package = "./proto";

// Example message
message Event {
	// city name
	string city = 1;
	// unix timestamp
	int64 timestamp = 2;
	// temperaure in Celcius
	int64 temperature = 3;
}